version: "3.7"
services:
  auto-deployment-agent:
    build:
      target: development
    image: deployment-agent:dev
    volumes:
      - ./deployment-agent:/devel/services/deployment-agent
