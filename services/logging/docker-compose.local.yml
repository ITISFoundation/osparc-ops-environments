version: "3.8"
services:
  mongodb:
    deploy:
      placement:
        constraints: []

  elasticsearch:
    deploy:
      placement:
        constraints: []

  graylog:
    deploy:
      placement:
        constraints: []
