### Docker Node Statistics Script

- This compiles available and used resources on docker swarm nodes, by comparing them with docker service limits
- This script needs to be run on a swarm manager node.
