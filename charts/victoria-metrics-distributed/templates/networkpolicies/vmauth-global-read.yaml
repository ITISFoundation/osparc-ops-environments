apiVersion: projectcalico.org/v3
kind: NetworkPolicy
metadata:
  name: vmauth-global-read-network-policy
spec:
  selector: app == "vmauth-global-read" && part-of == "{{ .Release.Name }}"
  ingress:
    - action: Allow
      protocol: TCP
      destination:
        ports:
          - {{ .Values.vmdistributed.common.vmauth.spec.port }}
  egress:
    - action: Allow
      protocol: TCP
      destination:
        ports:
          - {{ .Values.vmdistributed.common.vmauth.spec.port }}
